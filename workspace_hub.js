(() => {
  const API = () => `${window.BITEY_API_BASE || ''}/api/v1`;
  const modal = () => document.getElementById('modal-backdrop');
  const api = async (path, options={}) => { try { const r=await fetch(`${API()}${path}`,options); if(!r.ok)return null; return await r.json(); } catch { return null; } };
  const esc = s => String(s ?? '').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));
  const catalog = () => api('/workspace/catalog');
  const workspaces = async () => (await api('/workspaces'))?.workspaces || [];
  const getWorkspace = id => api(`/workspaces/${encodeURIComponent(id)}`);
  const createWorkspace = name => api('/workspaces',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,mode:'general',metadata:{interface:'bitey-web'}})});
  const createTask = (id,capability,prompt) => api(`/workspaces/${encodeURIComponent(id)}/tasks`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title:prompt.slice(0,80)||'Nueva tarea',prompt,capability,metadata:{interface:'bitey-web'}})});
  const runTask = (id,taskId) => api(`/workspaces/${encodeURIComponent(id)}/tasks/${encodeURIComponent(taskId)}/run`,{method:'POST',headers:{'Accept':'application/json'}});
  const icons={deep_research:'✦',browser_research:'⌕',documents:'▤',slides:'▥',spreadsheets:'▦',code:'⌘',files:'▣',projects:'◇',agents:'◈',chat:'◌'};
  const descriptions={chat:'Conversación y razonamiento con Bitey.',deep_research:'Investigación acotada con evidencia y fuentes.',browser_research:'Información web actual.',documents:'Documentos estructurados.',slides:'Presentaciones.',spreadsheets:'Hojas de cálculo y análisis.',code:'Generación y análisis de código.',files:'Contexto mediante archivos.',projects:'Contexto, tareas y entregables.',agents:'Orquestación especializada bajo las reglas de Bitey.'};
  const ensurePanel=()=>{let p=document.getElementById('workspace-panel');if(!p){p=document.createElement('section');p.id='workspace-panel';p.className='workspace-panel';p.setAttribute('aria-label','Bitey IA Workspace');const m=document.getElementById('messages');m?.parentNode.insertBefore(p,m); } return p;};
  const showPanel=()=>{const p=ensurePanel();p.hidden=false;document.getElementById('messages')?.setAttribute('hidden','');document.querySelector('.composer-wrap')?.setAttribute('hidden','');document.querySelector('.site-footer')?.setAttribute('hidden','');return p;};
  const hidePanel=()=>{const p=document.getElementById('workspace-panel');if(p)p.hidden=true;document.getElementById('messages')?.removeAttribute('hidden');document.querySelector('.composer-wrap')?.removeAttribute('hidden');document.querySelector('.site-footer')?.removeAttribute('hidden');};
  const close=()=>{modal()?.classList.remove('open');};
  function taskResultHtml(task){const r=task.result||{},e=r.evaluation||{},a=r.artifact,rs=r.research;return `<div class="workspace-result"><div class="workspace-result-head"><b>${esc(task.title||'Tarea')}</b><span class="workspace-badge">${esc(task.status||'completed')}</span></div>${e.decision?`<div class="workspace-research-meta"><b>Evaluación</b><span>${esc(e.decision)}${typeof e.confidence==='number'?` · ${Math.round(e.confidence*100)}%`:''}</span></div>`:''}${rs?`<div class="workspace-research-meta"><b>Investigación</b><span>${Array.isArray(rs.steps)?rs.steps.length:0} pasos acotados</span></div>`:''}${a?`<div class="workspace-artifact"><span>${icons[task.capability]||'▤'}</span><div><b>${esc(a.name||'Artefacto generado')}</b><small>${esc(a.artifact_type||'artifact')} · listo para revisión</small></div></div>`:''}<div class="workspace-answer">${esc(r.answer||'Sin contenido utilizable.')}</div></div>`;}
  async function loadHistory(id){const ws=await getWorkspace(id);const p=showPanel();const list=(ws?.tasks||[]).slice().reverse();const el=p.querySelector('#workspace-history');if(!el)return;el.innerHTML=list.length?list.map(t=>`<button class="workspace-history-item" data-task="${esc(t.id)}"><span class="workspace-history-icon">${icons[t.capability]||'◌'}</span><span><b>${esc(t.title||'Nueva tarea')}</b><small>${esc(t.status||'queued')} · ${esc(t.capability||'chat')}</small></span></button>`).join(''):'<div class="workspace-empty">Aún no hay tareas en este espacio.</div>';el.querySelectorAll('[data-task]').forEach(b=>b.addEventListener('click',()=>{const t=(ws?.tasks||[]).find(x=>x.id===b.dataset.task);if(t){p.querySelector('#workspace-result').innerHTML=taskResultHtml(t);p.querySelector('#workspace-result').hidden=false;}}));}
  async function runCapability(cap,ws){const p=showPanel();p.querySelector('#workspace-result').hidden=true;p.querySelector('#workspace-run')?.setAttribute('disabled','');p.querySelector('#workspace-run').textContent='Ejecutando…';const prompt=p.querySelector('#workspace-prompt').value.trim();if(!prompt)return;const task=await createTask(ws.id,cap.id,prompt);if(!task){p.querySelector('#workspace-run').textContent='Reintentar';p.querySelector('#workspace-error').textContent='No se pudo registrar la tarea.';p.querySelector('#workspace-error').hidden=false;return;}const result=await runTask(ws.id,task.id);p.querySelector('#workspace-run').removeAttribute('disabled');p.querySelector('#workspace-run').textContent='Ejecutar con Bitey';p.querySelector('#workspace-result').innerHTML=taskResultHtml(result||task);p.querySelector('#workspace-result').hidden=false;await loadHistory(ws.id);}
  async function hub(){const [c,all]=await Promise.all([catalog(),workspaces()]);let ws=all[0];if(!ws)ws=await createWorkspace('Espacio general');const caps=c?.capabilities||Object.keys(descriptions).map(id=>({id,label:id.replaceAll('_',' '),kind:'capability'}));const p=showPanel();p.innerHTML=`<div class="workspace-shell"><div class="workspace-header"><div><div class="eyebrow">BITEY IA WORKSPACE</div><h1>Tu espacio de trabajo</h1><p>Piensa, investiga, crea y ejecuta con el cerebro de Bitey.</p></div><div class="workspace-header-actions"><button id="workspace-back" class="secondary-action">Chat</button><button id="new-workspace" class="primary-action">＋ Nuevo espacio</button></div></div><div class="workspace-status"><span class="workspace-status-dot"></span><b>Modo gratuito protegido</b><span>· sin fallback de pago</span><span>· Supabase como memoria persistente</span></div><div class="workspace-layout"><div><div class="workspace-section-title">Capacidades</div><div class="workspace-grid">${caps.map(x=>`<button class="workspace-card" data-cap="${esc(x.id)}"><span class="workspace-icon">${icons[x.id]||'◌'}</span><b>${esc(x.label||x.id)}</b><small>${esc(descriptions[x.id]||x.kind||'Capacidad de Bitey IA')}</small></button>`).join('')}</div><div class="workspace-section-title">Nueva tarea</div><div class="workspace-task-form"><label for="workspace-prompt">¿Qué quieres que haga Bitey?</label><textarea id="workspace-prompt" rows="4" placeholder="Describe el resultado que necesitas..."></textarea><div id="workspace-error" class="workspace-error" hidden></div><div class="workspace-form-actions"><span id="workspace-selected">Selecciona una capacidad</span><button id="workspace-run" class="primary-action" disabled>Ejecutar con Bitey</button></div></div><div id="workspace-result" hidden></div></div><aside class="workspace-sidebar"><div class="workspace-section-title">Espacio activo</div><div class="workspace-current"><span class="workspace-icon">◇</span><div><b>${esc(ws.name)}</b><small>${all.length||1} espacios</small></div></div><div class="workspace-section-title">Historial de tareas</div><div id="workspace-history" class="workspace-history"></div></aside></div></div>`;
    let selected=null;p.querySelectorAll('[data-cap]').forEach(b=>b.addEventListener('click',()=>{selected=caps.find(x=>x.id===b.dataset.cap);p.querySelector('#workspace-selected').textContent=`Capacidad: ${selected.label||selected.id}`;p.querySelector('#workspace-run').disabled=false;p.querySelector('#workspace-prompt').focus();}));p.querySelector('#workspace-run').addEventListener('click',()=>selected&&runCapability(selected,ws));p.querySelector('#workspace-back').addEventListener('click',hidePanel);p.querySelector('#new-workspace').addEventListener('click',async()=>{const n=window.prompt('Nombre del espacio de Bitey IA');if(n?.trim()){await createWorkspace(n.trim());hub();}});await loadHistory(ws.id);}
  window.BiteyWorkspace={hub,catalog,workspaces,getWorkspace,createTask,runTask};window.BiteyUI=window.BiteyUI||{};window.BiteyUI.workspace=hub;
  document.addEventListener('DOMContentLoaded',()=>document.querySelectorAll('[data-workspace]').forEach(b=>b.addEventListener('click',e=>{e.preventDefault();hub();})));
})();
